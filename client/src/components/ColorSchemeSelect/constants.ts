import { rem } from '@mantine/core';

export const getButtonIconStyle = (size: number) => ({ width: rem(size), height: rem(size) });

export const MENU_ICON_STYLE = { width: rem(16), height: rem(16) };
